<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:WellnessWingman.PageModels"
             xmlns:models="clr-namespace:WellnessWingman.Models"
             xmlns:templates="clr-namespace:WellnessWingman.Pages.Templates"
             xmlns:gestures="clr-namespace:WellnessWingman.Utilities.Gestures"
             x:Class="WellnessWingman.Pages.MainPage"
             x:Name="MainPageRoot"
             x:DataType="pageModels:EntryLogViewModel"
             Title="Diet Tracker">

    <ContentPage.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="MealCardTemplate" x:DataType="models:MealPhoto">
                <Border StrokeThickness="1"
                        Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                        Padding="0"
                        Margin="0,0,0,16"
                        BackgroundColor="Transparent"
                        StrokeShape="RoundRectangle 12,12,12,12">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type pageModels:EntryLogViewModel}}, Path=SelectEntryCommand}" CommandParameter="{Binding .}" />
                    </Border.GestureRecognizers>

                    <Grid RowDefinitions="Auto,Auto" BackgroundColor="Transparent">
                        <Image Source="{Binding FullPath}"
                               Aspect="AspectFill"
                               HeightRequest="220"
                               SemanticProperties.Description="Meal photo" />

                        <Grid Grid.Row="0"
                              BackgroundColor="#80000000"
                              IsVisible="{Binding IsClickable, Converter={StaticResource InvertedBoolConverter}}">
                            <VerticalStackLayout HorizontalOptions="Center"
                                                 VerticalOptions="Center"
                                                 Spacing="8">
                                <ActivityIndicator IsRunning="True"
                                                   Color="White"
                                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                                <Label Text="Analyzing..."
                                       TextColor="White"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                                <Label Text="Analysis Failed - Tap to Retry"
                                       TextColor="Orange"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsFailedConverter}}" />
                                <Label Text="Analysis Skipped"
                                       TextColor="Gray"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsSkippedConverter}}" />
                            </VerticalStackLayout>
                        </Grid>

                        <VerticalStackLayout Padding="16" VerticalOptions="Start" Grid.Row="1" Spacing="4">
                            <Label Text="Meal captured"
                                   Style="{StaticResource Body2Strong}" />
                            <Label Text="{Binding Description}"
                                   Style="{StaticResource Body1}" />
                            <Label Text="{Binding LocalCapturedAt, StringFormat='Captured {0:MMM d, h:mm tt}'}"
                                   Style="{StaticResource Caption1}" />
                            <Label Text="Analysis Skipped"
                                   TextColor="{StaticResource Gray500}"
                                   FontAttributes="Bold"
                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsSkippedConverter}}" />
                            <Label Text="Processing..."
                                   TextColor="{StaticResource Gray500}"
                                   FontAttributes="Bold"
                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                            <Label Text="Failed"
                                   TextColor="{StaticResource Error}"
                                   FontAttributes="Bold"
                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsFailedConverter}}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="ExerciseCardTemplate" x:DataType="models:ExerciseEntry">
                <Border StrokeThickness="1"
                        Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                        Padding="0"
                        Margin="0,0,0,16"
                        BackgroundColor="Transparent"
                        StrokeShape="RoundRectangle 12,12,12,12">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type pageModels:EntryLogViewModel}}, Path=SelectEntryCommand}" CommandParameter="{Binding .}" />
                    </Border.GestureRecognizers>

                    <Grid RowDefinitions="Auto,Auto" BackgroundColor="Transparent">
                        <Image Source="{Binding PreviewPath}"
                               Aspect="AspectFill"
                               HeightRequest="220"
                               SemanticProperties.Description="Exercise screenshot" />

                        <Grid Grid.Row="0"
                              BackgroundColor="#80000000"
                              IsVisible="{Binding IsClickable, Converter={StaticResource InvertedBoolConverter}}">
                            <VerticalStackLayout HorizontalOptions="Center"
                                                 VerticalOptions="Center"
                                                 Spacing="8">
                                <ActivityIndicator IsRunning="True"
                                                   Color="White"
                                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                                <Label Text="Analyzing..."
                                       TextColor="White"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                                <Label Text="Analysis Failed - Tap to Retry"
                                       TextColor="Orange"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsFailedConverter}}" />
                                <Label Text="Analysis Skipped"
                                       TextColor="Gray"
                                       FontAttributes="Bold"
                           IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsSkippedConverter}}" />
                        </VerticalStackLayout>
                    </Grid>

                    <VerticalStackLayout Padding="16" VerticalOptions="Start" Grid.Row="1" Spacing="4">
                        <Label Text="Exercise entry"
                               Style="{StaticResource Body2Strong}" />
                        <Label Text="{Binding Description}"
                               Style="{StaticResource Body1}" />
                        <Label Text="{Binding LocalCapturedAt, StringFormat='Captured {0:MMM d, h:mm tt}'}"
                               Style="{StaticResource Caption1}" />
                            <Label Text="Analysis Skipped"
                                   TextColor="{StaticResource Gray500}"
                                   FontAttributes="Bold"
                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsSkippedConverter}}" />
                            <Label Text="Processing..."
                                   TextColor="{StaticResource Gray500}"
                                   FontAttributes="Bold"
                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                            <Label Text="Failed"
                                   TextColor="{StaticResource Error}"
                                   FontAttributes="Bold"
                           IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsFailedConverter}}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="SleepCardTemplate" x:DataType="models:SleepEntry">
                <Border StrokeThickness="1"
                        Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                        Padding="0"
                        Margin="0,0,0,16"
                        BackgroundColor="Transparent"
                        StrokeShape="RoundRectangle 12,12,12,12">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type pageModels:EntryLogViewModel}}, Path=SelectEntryCommand}" CommandParameter="{Binding .}" />
                    </Border.GestureRecognizers>

                    <Grid RowDefinitions="Auto,Auto" BackgroundColor="Transparent">
                        <Image Source="{Binding PreviewPath}"
                               Aspect="AspectFill"
                               HeightRequest="220"
                               SemanticProperties.Description="Sleep screenshot" />

                        <Grid Grid.Row="0"
                              BackgroundColor="#80000000"
                              IsVisible="{Binding IsClickable, Converter={StaticResource InvertedBoolConverter}}">
                            <VerticalStackLayout HorizontalOptions="Center"
                                                 VerticalOptions="Center"
                                                 Spacing="8">
                                <ActivityIndicator IsRunning="True"
                                                   Color="White"
                                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                                <Label Text="Analyzing..."
                                       TextColor="White"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                                <Label Text="Analysis Failed - Tap to Retry"
                                       TextColor="Orange"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsFailedConverter}}" />
                                <Label Text="Analysis Skipped"
                                       TextColor="Gray"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsSkippedConverter}}" />
                            </VerticalStackLayout>
                        </Grid>

                        <VerticalStackLayout Padding="16" VerticalOptions="Start" Grid.Row="1" Spacing="4">
                            <Label Text="Sleep entry"
                                   Style="{StaticResource Body2Strong}" />
                            <Label Text="{Binding Description}"
                                   Style="{StaticResource Body1}" />
                            <Label Text="{Binding LocalCapturedAt, StringFormat='Captured {0:MMM d, h:mm tt}'}"
                                   Style="{StaticResource Caption1}" />
                            <Label Text="Analysis Skipped"
                                   TextColor="{StaticResource Gray500}"
                                   FontAttributes="Bold"
                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsSkippedConverter}}" />
                            <Label Text="Processing..."
                                   TextColor="{StaticResource Gray500}"
                                   FontAttributes="Bold"
                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsProcessingConverter}}" />
                            <Label Text="Failed"
                                   TextColor="{StaticResource Error}"
                                   FontAttributes="Bold"
                                   IsVisible="{Binding ProcessingStatus, Converter={StaticResource IsFailedConverter}}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>
            </DataTemplate>

            <templates:EntryCardTemplateSelector x:Key="EntryCardTemplateSelector"
                                                 MealTemplate="{StaticResource MealCardTemplate}"
                                                 ExerciseTemplate="{StaticResource ExerciseCardTemplate}"
                                                 SleepTemplate="{StaticResource SleepCardTemplate}" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Spacing="24" Padding="24,32">
            <VerticalStackLayout.Behaviors>
                <gestures:ImprovedHorizontalSwipeBehavior SwipeLeftCommand="{Binding SwipeToWeekCommand}" />
            </VerticalStackLayout.Behaviors>
            <VerticalStackLayout Spacing="8">
                <Label Text="Capture Your Health Entries" Style="{StaticResource Headline}" />
            </VerticalStackLayout>

            <Button Text="Take Photo"
                    SemanticProperties.Description="Capture a meal photo"
                    Clicked="TakePhotoButton_Clicked" />

            <Border StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                    Padding="16"
                    Margin="0,8,0,0"
                    BackgroundColor="Transparent"
                    StrokeShape="RoundRectangle 12,12,12,12"
                    IsVisible="{Binding HasCompletedMeals}">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Today's Nutrition" Style="{StaticResource Body2Strong}" />

                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,*,*,*" RowSpacing="12">
                        <!-- Row 0: Macros -->
                        <VerticalStackLayout Grid.Row="0" Grid.Column="0">
                            <Label Text="{Binding LiveNutritionalTotals.Calories, StringFormat='{0:F0}'}" FontAttributes="Bold" HorizontalOptions="Center" FontSize="18"/>
                            <Label Text="Cals" Style="{StaticResource Caption1}" HorizontalOptions="Center" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="0" Grid.Column="1">
                            <Label Text="{Binding LiveNutritionalTotals.Protein, StringFormat='{0:F0}g'}" FontAttributes="Bold" HorizontalOptions="Center" FontSize="18"/>
                            <Label Text="Prot" Style="{StaticResource Caption1}" HorizontalOptions="Center" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="0" Grid.Column="2">
                            <Label Text="{Binding LiveNutritionalTotals.Carbohydrates, StringFormat='{0:F0}g'}" FontAttributes="Bold" HorizontalOptions="Center" FontSize="18"/>
                            <Label Text="Carbs" Style="{StaticResource Caption1}" HorizontalOptions="Center" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="0" Grid.Column="3">
                            <Label Text="{Binding LiveNutritionalTotals.Fat, StringFormat='{0:F0}g'}" FontAttributes="Bold" HorizontalOptions="Center" FontSize="18"/>
                            <Label Text="Fat" Style="{StaticResource Caption1}" HorizontalOptions="Center" />
                        </VerticalStackLayout>

                        <!-- Row 1: Micros -->
                        <VerticalStackLayout Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="Center">
                            <Label Text="{Binding LiveNutritionalTotals.Fiber, StringFormat='{0:F0}g'}" FontAttributes="Bold" HorizontalOptions="Center" FontSize="16"/>
                            <Label Text="Fiber" Style="{StaticResource Caption1}" HorizontalOptions="Center" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" HorizontalOptions="Center">
                            <Label Text="{Binding LiveNutritionalTotals.Sugar, StringFormat='{0:F0}g'}" FontAttributes="Bold" HorizontalOptions="Center" FontSize="16"/>
                            <Label Text="Sugar" Style="{StaticResource Caption1}" HorizontalOptions="Center" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" HorizontalOptions="Center">
                            <Label Text="{Binding LiveNutritionalTotals.Sodium, StringFormat='{0:F0}mg'}" FontAttributes="Bold" HorizontalOptions="Center" FontSize="16"/>
                            <Label Text="Sodium" Style="{StaticResource Caption1}" HorizontalOptions="Center" />
                        </VerticalStackLayout>
                    </Grid>

                    <Button Text="Get More Details"
                            Command="{Binding GenerateDailySummaryCommand}"
                            IsVisible="{Binding ShowGenerateSummaryButton}"
                            IsEnabled="{Binding IsGeneratingSummary, Converter={StaticResource InvertedBoolConverter}}" />

                    <Button Text="View Analysis"
                            Command="{Binding ViewDailySummaryCommand}"
                            IsVisible="{Binding ShowViewAnalysisButton}" />

                    <VerticalStackLayout Spacing="4" 
                                         IsVisible="{Binding IsSummaryProcessing}" 
                                         HorizontalOptions="Center">
                        <ActivityIndicator IsRunning="True" Color="{StaticResource Primary}" />
                        <Label Text="Generating analysis..." 
                               Style="{StaticResource Caption1}" 
                               TextColor="{StaticResource Gray500}" 
                               HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Label Text="Recent Entries"
                   Style="{StaticResource Headline}"
                   Margin="0,24,0,0" />

            <VerticalStackLayout BindableLayout.ItemsSource="{Binding Entries}"
                                 BindableLayout.ItemTemplateSelector="{StaticResource EntryCardTemplateSelector}"
                                 Spacing="0"
                                 Margin="0,8,0,0">
                 <BindableLayout.EmptyView>
                    <VerticalStackLayout Padding="32" Spacing="12" HorizontalOptions="Center" VerticalOptions="Center">
                        <Label Text="No entries tracked yet." HorizontalTextAlignment="Center" Style="{StaticResource Body1}" />
                        <Label Text="Snap a photo or share a workout to see it here." HorizontalTextAlignment="Center" Style="{StaticResource Caption1}" />
                    </VerticalStackLayout>
                </BindableLayout.EmptyView>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
