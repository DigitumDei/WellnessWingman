# Review Follow-up

## Scope
- Address unresolved PR #137 review feedback around nutritional profile archive paths and migration helper clarity.

## Changes
- Refactored migration path handling to use `resolveAbsolutePath`, so export and import both operate on explicitly absolute file paths and export no longer re-resolves the same source image path twice.
- Made external nutritional profile archive keys collision-resistant by appending a stable suffix derived from the raw profile identity, preventing ZIP path collisions when different `externalId` values sanitize to the same segment.
- Expanded tests to cover sanitized `externalId` collisions, blank `externalId` fallback behavior, and the updated archive-path expectations for external image exports.

## Verification
- Added targeted unit coverage in `DataMigrationServiceTest`.
- Local Gradle execution remains best-effort on this VM; PR CI should be treated as the authoritative validation pass.
