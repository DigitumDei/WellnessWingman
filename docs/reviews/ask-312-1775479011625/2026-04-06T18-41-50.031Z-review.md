# Follow-up Review

- Branch: ask/312-1775479011625
- Base: origin/master
- Verdict: pass pending CI

## Addressed Issues

- Fixed nutritional-profile export/import round-tripping for source images outside `appDataDir`. External absolute paths are now rewritten to stable relative archive paths under `nutritional-profiles/<profile-key>/...`, so restored profiles point to files inside app data instead of stale machine-local paths.
- Fixed basename collisions for external nutritional-profile images. Distinct profiles no longer deduplicate to the same ZIP entry when two external files share the same filename.
- Preserved blank `sourceImagePath` values during export instead of rewriting them into synthetic archive paths.
- Strengthened the fake nutritional profile repository to behave like an upsert target rather than an append-only sink, and added a controlled failure mode for import-loop error assertions.

## Added Regression Coverage

- Export rewrites external nutritional-profile image paths to stable archive paths.
- Export preserves distinct archive paths for external-image basename collisions.
- Export skips missing nutritional-profile image files while keeping the rewritten archive path in `data.json`.
- Export leaves blank nutritional-profile image paths untouched and avoids file export work.
- Import rewrites exported external nutritional-profile image paths into app data on restore.
- Import continues after a nutritional-profile upsert failure and reports partial success correctly.
- Legacy archive compatibility test now asserts that the entry payload still imports, not only that the new profile list is absent.

## Verification

- Attempted local verification with `./gradlew :shared:desktopTest --tests 'com.wellnesswingman.domain.migration.DataMigrationServiceTest'`.
- This VM did not provide a usable completion signal or test artifacts during the run window, so local verification remains inconclusive.
- CI on PR #137 should be treated as the authoritative validation pass for this follow-up.
